{
  "name": "docus-starter",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt generate",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "docus": "^0.0.3",
    "nuxt": "^2.14.12"
  },
  "devDependencies": {
    "eslint-config-prettier": "^9.1.0",
    "push-dir": "^0.4.1"
  }
}
